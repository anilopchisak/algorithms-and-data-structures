# 📘 Шпаргалка по алгоритмам сортировки

https://proglib.io/p/sort-gif

## 📌 Пузырьковая сортировка (Bubble Sort)
**Как работает:** многократно сравнивает соседние элементы и меняет их местами.  
**Проходы:** до `n–1`  
**Остановка:** проход без обменов  
**Особенность (суть алгоритма):**
- двойной `for`, внутри `if + swap`
- повторяем, пока «пузырьки» всплывают

**Сложность:**
- Лучший: `O(n)`
- Средний: `O(n²)`
- Худший: `O(n²)`

---

## 📌 Сортировка выбором (Selection Sort)
**Как работает:** на каждом шаге ищет минимум в оставшейся части.  
**Проходы:** `n–1`  
**Остановка:** выбран каждый минимум  
**Особенность:**
- внешний `for` по позициям
- внутренний `for` ищет минимум
- затем `swap`

**Сложность:**
- Всегда: `O(n²)`

---

## 📌 Сортировка вставками (Insertion Sort)
**Как работает:** вставляет элемент в отсортированную часть слева.  
**Проходы:** `n–1`  
**Остановка:** массив пройден  
**Особенность:**
- `for` по i
- внутри `while`, который сдвигает элементы вправо

**Сложность:**
- Лучший: `O(n)`
- Средний: `O(n²)`
- Худший: `O(n²)`

---

## 📌 Вставки с бинарным поиском (Binary Insertion Sort)
**Как работает:** ищем позицию бинарным поиском, затем сдвигаем элементы.  
**Особенность:**
- `for`, внутри: `binary search` → `while shift`

**Сложность:**
- `O(n²)` (сдвиги элементов остаются)

---

## 📌 Быстрая сортировка (Quick Sort)
**Как работает:** выбирает опорный элемент, делит массив, рекурсивно сортирует части.  
**Остановка:** подмассив ≤ 1  
**Особенность:**
- рекурсия
- функция `partition`, разбивает массив на две части

**Сложность:**
- Лучший: `O(n log n)`
- Средний: `O(n log n)`
- Худший: `O(n²)`

---

## 📌 Сортировка слиянием (Merge Sort)
**Как работает:** делит массив пополам, сортирует части, затем сливает.  
**Остановка:** подмассив ≤ 1  
**Особенность:**
- рекурсия `divide`
- слияние: два указателя двигаются навстречу

**Сложность:**
- Всегда: `O(n log n)`

---

## 📌 Пирамидальная сортировка (Heap Sort)
**Как работает:** строит кучу и извлекает максимум.  
**Остановка:** куча разобрана  
**Особенность:**
- `heapify` — просеивание вниз
- цикл извлечения max

**Сложность:**
- Всегда: `O(n log n)`

---

## 📌 Поразрядная сортировка (Radix Sort)
**Как работает:** сортирует числа по разрядам.  
**Остановка:** обработаны все разряды  
**Особенность:**
- внешний цикл по разрядам
- внутри стабильная сортировка (обычно Counting Sort)

**Сложность:**
- `O(nk)`  
  (k — количество разрядов)

---

## 📌 Сортировка подсчётом (Counting Sort)
**Как работает:** считает количество появлений значений и собирает массив.  
**Особенность:**
- цикл подсчёта
- цикл построения префиксов
- цикл сборки результата

**Сложность:**
- `O(n + k)`

---

## 📌 Шейкерная сортировка (Cocktail Sort)
**Как работает:** улучшенный пузырёк — проходы слева направо и справа налево.  
**Остановка:** нет обменов в обе стороны  
**Особенность:**
- прямой проход
- обратный проход  
  в одном цикле

**Сложность:**
- Лучший: `O(n)`
- Средний: `O(n²)`
- Худший: `O(n²)`

---

## 📌 Comb Sort
**Как работает:** сравнивает элементы через уменьшающийся шаг.  
**Остановка:** gap = 1 и нет обменов  
**Особенность:**
- `while`
- сравнение элементов через gap
- gap уменьшается: `gap *= 0.85`

**Сложность:**
- Средний/худший: `O(n²)`

---

## 📌 Shell Sort
**Как работает:** выполняет вставки на подмассивах с шагом gap.  
**Остановка:** gap = 1 и массив отсортирован  
**Особенность:**
- цикл по gap
- внутри алгоритм вставок для элементов через gap

**Сложность:**
- Средний: ~ `O(n^1.3)`
- Худший: `O(n²)`

---
